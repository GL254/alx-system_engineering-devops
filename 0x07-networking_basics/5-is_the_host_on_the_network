#!/usr/bin/env bash
# This script will ping IP address passed as an argument


if [ $# -eq 0 ]
then
	echo "Usage: 5-is_the_host_on_the_network {IP_ADDRESS}"
else
	function pinging()
	{
		ping -c 5 "$@"
	}
	pinging "$@"
fi
